---
status: accepted
---

# After install, run `next typegen` on Next.js projects

After install, a Next.js destination still runs `next typegen`. Warn and continue if the CLI has no such command. Fail the run if typegen itself fails.
